# Build
This project uses [Eleventy](https://www.11ty.dev/) as a static site generator.

To build the site locally, run:

1. Run `npm install`
1. Run `npm start`
1. Navigate to the demo URL at `http://localhost:8888/`

Navigate to the demo URL at http://localhost:8888/
